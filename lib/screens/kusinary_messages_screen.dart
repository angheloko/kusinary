import 'package:flutter/material.dart';

class KusinaryMessagesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox.expand(
      child: Container(
        color: Colors.orange,
      ),
    );
  }
}
